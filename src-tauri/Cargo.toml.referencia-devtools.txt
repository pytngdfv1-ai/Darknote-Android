# ================================================================
# REFERENCIA — NO reemplaces tu Cargo.toml completo con esto.
# Solo copia la línea de "tauri" con la feature "devtools" añadida
# a TU Cargo.toml real, dentro de [dependencies].
# ================================================================
#
# Busca esta línea en tu src-tauri/Cargo.toml:
#
#   tauri = { version = "2", features = [] }
#
# y cámbiala temporalmente por:
#
#   tauri = { version = "2", features = ["devtools"] }
#
# Esto habilita el clic derecho -> "Inspeccionar" incluso en un
# build de release, para poder ver la consola de JavaScript y
# el error EXACTO que está impidiendo que respondan los botones.
#
# Una vez identifiques y resuelvas el problema, puedes quitar
# la feature "devtools" para la versión final que distribuyas.
# ================================================================
